import type { BlogPost } from "@/types/blog";

// Add one import + array entry per post as content is written, e.g.:
//   import { myFirstPost } from "./my-first-post";
//   export const posts: BlogPost[] = [myFirstPost];
export const posts: BlogPost[] = [];
